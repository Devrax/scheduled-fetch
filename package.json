{
  "name": "waiterfetch",
  "version": "0.0.1",
  "description": "A wrapper around fetch for scheduling request and caching in local (Capacitor preference) each last response",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "fetch"
  ],
  "author": "Rafael Mejia",
  "license": "MIT",
  "dependencies": {
    "@capacitor/preferences": "^4.0.1",
    "rxjs": "^7.5.7"
  }
}
